# Runtime dependencies for the PACE_PLUS multimodal pipeline.
# Install a CUDA-compatible PyTorch build when the default wheel does not match
# the driver on the target machine.
torch==2.11.0
torchvision==0.26.0
transformers==5.16.1
vllm==0.20.2
ray[default]==2.58.0
tensordict==0.10.0
datasets==5.0.1
PyYAML>=6.0
numpy>=2.0,<3
Pillow>=10.0
qwen-vl-utils==0.0.14
hydra-core>=1.3
scikit-learn>=1.4
tqdm>=4.66
packaging>=24.0
